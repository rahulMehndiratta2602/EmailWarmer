    //Implement get list of proxies from abc proxy
    //Create an endpoint to get the list of proxies
    //Create a function to get the list of proxies
    //Create a function to save the list of proxies to the database
    //Create a function to get the list of proxies from the database
    //Create a function to delete the list of proxies from the database
    //Create a function to update the list of proxies in the database
    //Create a function to get the list of proxies from the database
    


//Impement a function that takes a list of user emails and password object array a list of proxies and max numner of proxies to use and maximum number of emails per proxy and creates a mapping of email to proxy . This will be a one to many mapping. One proxy can have lot of emails opened on it at the same time. but one email can be opened from only one proxy at all times and never from any other proxy.
    
// Create a function that extracts list of emails and passwords from the db and the proxy mapping and opens a google browser window for each email and password pair on the proxy

// Create a complete api documentation for the codebase